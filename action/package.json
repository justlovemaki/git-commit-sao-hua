{
  "name": "git-commit-sao-hua-action",
  "version": "1.24.0",
  "description": "GitHub Action for generating sao hua commit messages (with AI support)",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build index.js -o dist",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "@actions/core": "^1.10.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
